# Gold layer

Business KPI Delta tables under `datasets/delta/gold/`.

- `city_pulse_snapshot` — active alert counts, avg precip, `city_stress_score`
- `transit_alert_summary` — alert counts by category
