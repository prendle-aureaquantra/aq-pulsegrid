# Copy to deploy.config.env (gitignored — never commit real values).

AQ_LIGHTSAIL_HOST=
AQ_LIGHTSAIL_INSTANCE_NAME=

AQ_LIGHTSAIL_USER=bitnami
AQ_LIGHTSAIL_REMOTE_DIR=/var/aq-pulsegrid

AQ_LIGHTSAIL_KEY=secrets/lightsail-key.pem

AWS_PROFILE=
